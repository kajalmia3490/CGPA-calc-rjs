import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <div>
            <button
                type="submit"
            >
                Calculate
            </button>
        </div>
    );
};

export default Button;